/* CREATE TABLE cart(id INTEGER PRIMARY KEY, name TEXT, price INTEGER,
 stock DOUBLE, imageUrl) */

/*SELECT FROM TABLE cart WHERE email = $email;

/* INSERT INTO cart(id, name, price, stock, imageUrl) VALUES
   (?, ?, ?, ?, ?) */

/* CREATE TABLE order(id, name, total) */
